import React from 'react';
import Address from '../Components/Address';

const Home = (props) => {
    console.log(props);
    return (
        <>
            <Address />
        </>
    );
};

export default Home;
